## Summary
- **Grok Bot 双轨**：按本机 Cursor 版本走 3.18 或 3.19 补丁；3.18 补上 4.6/4.5 prompt 互斥和 agent-host 分包，3.19 用另一套 L1/L2/L6 形态，避免「显示已注入、实际仍走 RunInference」
- **写入过程**：Bot 面板逐文件显示步骤；还原 Bot 会尽量留下 MAX 和 500k
- **明细**：分类折叠、按文字宽度收徽章；「会员/目录回包」是完整解锁的 fetch，不归 Grok Bot，行内「去完整解锁」跳到急救补丁里的真按钮
- **旧版风格**：只有 IDE 开着却没带 `--classic` 才提示，不再占健康条标题

## 产物
- `CursorLauncher.exe` / `CursorLauncherSetup.exe`

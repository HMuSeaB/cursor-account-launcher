## Summary
- **双网关诊断**：面板分开显示 YC 原生（`43111/__bajie`，模型多 / 自己的号 / Sand）和 Sub2API（localhost 凭据，窄模型墙）；叠打或都没接管时写明怎么切，不再把 Sub2API 误报成「模型墙掉了」
- **桌面图标**：快捷方式改用带版本号的 `.ico`，启动时刷新已有 `.lnk`，避免 Windows 缓存导致文件夹里 exe 图标已换、桌面还是旧的
- 16/24px 图标补上播放键，资源管理器列表视图不再只剩一根竖条

## 产物
- `CursorLauncher.exe` / `CursorLauncherSetup.exe`

## 图标
装好或覆盖绿色版后**打开一次启动器**（或设置 → 快捷方式 → 刷新图标）。安装包创建的桌面快捷方式已指向本版 `app-1.3.10.ico`。
